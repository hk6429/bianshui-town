# E20 局部場景更新

- main.applyUrban 交易 prepare 直接同步草稿，不 reset 整城；失敗時同步原始 Town。
- TownScene 按 Town identity/revision 啟動檢查，再分別比較道路拓樸、建築幾何輸入與花草遮擋簽章。施工轉階／普通升級不重建道路、花草與未改建築。建築簽章涵蓋 type/variant/footprint/院落縮放。新模型成功後才釋放舊模型，失敗則使同步檢查失效以允許復原。
- 生活裝飾依位置／造型／院落更新；移除不再存在的裝飾、工人、訪客與文人，避免拿掉全場 reset 後留下殘影或存取不存在建築。共享院落按實際布局及完工狀態判定。
- Node 聚焦與全套輸出附檔；全套316通過，build成功。scene-sync.test.js 使用真實 Three 模型驗證 identity、道路替換、移動拆除與模型建立失敗後復原。
- Chrome 使用獨立67棟城鎮測試，不讀寫使用者存檔。18項通過；browser.json含原始檢查與 updateMs。一次升級後其餘66棟同一物件，實際tick跨施工階段只換工坊，道路／花草維持 identity；移動裝飾、拆除工人、道路變更與存檔失敗後原城恢復皆通過。
- 實測 update（含當幀 render）約165–859ms，非純同步成本，也未與舊版同機受控比較；證據支持減少重建，不宣稱穩定60fps或零延遲。
