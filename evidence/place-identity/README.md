# 城鎮命名、地標命名與書籤 O10–O12

- 6個新案例；identity/schema/transaction聚焦66/66，完整282/282。之後只修手機CSS：最長名稱省略、數字三欄、概況層級避免場景標籤覆蓋；最終build成功。
- 命名1–24字，trim但拒絕純空白/控制字元/過長，儲存schema同樣驗證。城鎮名稱在概況與成果對照回顯，汴水小鎮品牌保留。
- 建物保留originalName作原型，自訂name沿用既有資訊/目的地查詢。升級及搬移保留自訂名；舊日記及已完成委託文字是歷史快照，不重寫。
- 書籤依building ID定位，最多16份，可獨立命名/移除。搬移跟隨新位置，拆除或合併消失的ID顯示失效且不可跳轉，不猜其他地點。
- Chrome隔離fixture：空白名稱被拒絕、柳岸小城概況回顯、街角書肆→聽雨書齋且居民前往名稱同步（names.txt）。午後讀書/遊園處各別跳轉，移除讀書書籤保留園景（bookmarks.txt）；園景拆除後失效disabled（demolished.txt）。
- 手機390×844、24字最長名稱，概況一列名稱/三欄數字，mobile.png可讀且無場景標籤遮擋。viewport已reset，測試分頁已關閉。
- 儲存失敗不發布名稱/書籤；舊檔不必新增欄位；HTML-like名稱跳脫測試通過。
