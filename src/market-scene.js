import * as THREE from 'three';import {marketStalls,marketOpen} from './market.js';import {streetHeight} from './life.js';
export class MarketScene{
 constructor(owner){this.owner=owner;this.models=new Map();}
 create(s){const g=new THREE.Group(),box=(w,h,d,c,x,y,z)=>{const m=new THREE.Mesh(new THREE.BoxGeometry(w,h,d),new THREE.MeshStandardMaterial({color:c,roughness:1}));m.position.set(x,y,z);m.castShadow=true;g.add(m);return m;};box(1.1,.12,.65,0x886b4b,.45,.72,0);for(const x of [0,.9])box(.065,.65,.065,0x755a40,x,.35,0);for(const x of [-.1,1])box(.06,1.7,.06,0x755a40,x,.85,-.2);box(1.35,.065,1,s.color,.45,1.75,0);if(s.id==='paper'){for(let i=0;i<3;i++)box(.23,.04,.48,0xdaca9e,.12+i*.32,.82,0);}else for(let i=0;i<6;i++){const m=new THREE.Mesh(new THREE.SphereGeometry(s.id==='fruit'?.09:.065,7,5),new THREE.MeshStandardMaterial({color:s.id==='fruit'?(i%2?0xcaa86b:0xa78651):s.id==='tea'?0x617d71:0xe1c799}));m.position.set(.12+(i%3)*.3,.84,Math.floor(i/3)*.22-.1);g.add(m);}const person=this.owner.person(98000+this.models.size);person.position.set(.45,0,-.65);g.add(person);g.userData.human=person;g.traverse(o=>o.userData.stallId=s.id);this.owner.scene.add(g);return g;}
 update(t){const stalls=marketStalls(t),ids=new Set(stalls.map(s=>s.id));for(const [id,g] of this.models)if(!ids.has(id)){this.dispose(g);this.models.delete(id);}for(const s of stalls){let g=this.models.get(s.id);if(!g){g=this.create(s);this.models.set(s.id,g);}g.position.set(s.x,streetHeight(s.x,s.z),s.z);g.userData.human.visible=marketOpen(t);this.owner.lifeScene.animateHuman(g.userData.human,t.elapsed,false,marketOpen(t));}}
 dispose(g){this.owner.scene.remove(g);g.traverse(o=>{o.geometry?.dispose();});}
 reset(){for(const g of this.models.values())this.dispose(g);this.models.clear();}
}
