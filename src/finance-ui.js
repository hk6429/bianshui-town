import {formatMoney} from './money.js';
import {wellbeingReport} from './wellbeing.js';
import {gardenReport} from './garden-services.js';
import {pollutionReport} from './pollution.js';
import {educationReport} from './education.js';
import {healthcareReport} from './healthcare.js';
import {fireCoverage} from './fire-service.js';
import {sanitationReport} from './sanitation.js';
import {waterReport} from './water-service.js';
import {logistics,upgradeLogistics,MAX_LOGISTICS_LEVEL} from './logistics.js';
import {GOODS} from './production.js';
import {affordable} from './city-finance.js';
import {cityDemand,CENSUS_SECONDS,GRACE} from './city-growth.js';
import {BUILD_COST,ROAD_COST,UPKEEP,dailyUpkeep,householdTax,setCityPolicy,buyLand} from './city-finance.js';
import {townBounds,boundsCells,landCost,expansionOf,MAX_EXPANSION} from './grid-rules.js';
import {transitTaxRate,wineExcise,wineShops,dockBonus,granaryStores,hasGovernment} from './civic.js';
export function installFinanceUI({getTown,edit}){
 const $=s=>document.querySelector(s),dialog=$('#city-budget');
 function growth(){const t=getTown(),d=cityDemand(t),root=$('#city-services'),demandRoot=$('#city-demand');root.replaceChildren();demandRoot.replaceChildren();const water=waterReport(t),summary=document.createElement('p');summary.id='water-summary';summary.textContent=`街坊供水：${water.served}／${water.residents} 位住戶；有水空位 ${water.available} 席；供水滿意度 ${water.satisfaction===null?'尚無住戶':water.satisfaction+'％'}。水井 ${water.wells.length} 處。` ;root.append(summary);const hygiene=sanitationReport(t),clean=document.createElement('p');clean.id='sanitation-summary';clean.textContent=`衛生 ${hygiene.hygiene}／100；平均健康 ${hygiene.health}／100；積存髒污 ${hygiene.waste} 份，生產待結算 ${hygiene.pending} 份。公共服務效率 ${Math.round(hygiene.efficiency*100)}％；清運院 ${hygiene.yards.length} 處，每日最多處理 ${hygiene.yards.reduce((n,y)=>n+y.capacity,0)} 份（須在沿路範圍內）。每日每位住戶產生1份髒污、每件成品2份；未處理會逐日降低健康與住宅需求，清乾淨後健康逐日回復。`;root.append(clean);const fire=fireCoverage(t),fireSummary=document.createElement('p');fireSummary.textContent=`防火巡守：${fire.covered.size}／${fire.buildings.length} 處建築；巡守所 ${fire.posts.length} 處。窯坊與密集建築提高風險；前兩日免事件，此後每日最多一處30秒預警，可點上方火警提示處置。受損保留住戶與貨物，暫停營業、生產，倒數結束後免費恢復。`;root.append(fireSummary);const medical=healthcareReport(t),medicalSummary=document.createElement('p');medicalSummary.id='medical-summary';medicalSummary.textContent=`藥鋪醫療：照護 ${medical.assigned.size} 人、待照護 ${medical.waiting} 人；病假 ${medical.sick} 人。${medical.clinics.length} 間落成藥鋪，同時照護容量 ${medical.clinics.reduce((n,c)=>n+c.capacity,0)} 人。需營業且健康過賣到場；每名過賣每級照護4人（四格8人），沿住家步道24步起，照護每30秒恢復8點健康。健康未滿40請假，恢復40即可返工。自由營造不結算健康與病假。`;root.append(medicalSummary);const education=educationReport(t),educationSummary=document.createElement('p');educationSummary.id='education-summary';educationSummary.textContent=`書院教育：受教 ${education.assigned.size} 人、未受教 ${education.waiting} 人；平均學力 ${education.average.toFixed(1)}／100。書院 ${education.academies.length} 處，每級8個同時受教名額；欠款會減少名額，受損停課。依住家道路覆蓋、優先低學力，持續涵蓋一日增加10點；滿100不再占名額。到場工匠平均學力提供最高20％產能加成。自由營造不累積學力，也不套用產能加成。`;root.append(educationSummary);const pollution=pollutionReport(t),pollutionSummary=document.createElement('p');pollutionSummary.id='pollution-summary';pollutionSummary.textContent=`生產污染：住宅平均暴露 ${pollution.exposure.toFixed(1)}／100，居住環境 ${pollution.environment.toFixed(1)}／100；環境分項納入民生滿意。按住宅住戶數加權，空屋算1；每件陶器18、木器8、布匹14排放強度，向三格內隨距離衰減。停工後每90遊戲秒減半，搬遷或拆除後原址也需逐步恢復。可把工坊與住宅隔開，缺料、無工匠或受損停工不新增排放。`;root.append(pollutionSummary);const gardens=gardenReport(t),gardenSummary=document.createElement('p');gardenSummary.id='garden-summary';gardenSummary.textContent=`園景宜居：${gardens.gardens} 處可用園景，涵蓋 ${gardens.covered}／${gardens.homes.length} 處住宅；平均加成 ${gardens.average.toFixed(1)}／20，環境分項納入民生滿意。依住宅住戶數加權（空屋1），須沿路可達且在範圍內；第二處效益折半、第三處四分之一，堆疊上限20。距離、級數、維護效率皆影響效益；受損園景暫停服務。`;root.append(gardenSummary);const wellbeing=wellbeingReport(t),wellbeingSummary=document.createElement('p');wellbeingSummary.id='wellbeing-summary';wellbeingSummary.textContent=`民生滿意 ${wellbeing.average.toFixed(1)}／100，偏低 ${wellbeing.low} 人；住宅綜合需求修正 ${wellbeing.demandModifier}。住房20％、就業15％、採買15％、公共服務25％、環境25％；各居民小卡可查分項原因。連續低於65分達600遊戲秒後可能遷出，改善至65分即清除該倒數；每次人口評估最多一人。供水、衛生、污染與園景透過此分數影響需求，不再重複加減。自由營造只參考，不觸發遷出。`;root.append(wellbeingSummary);for(const [key,name] of [['home','住宅'],['shop','商業'],['work','作坊']]){const row=document.createElement('p'),label=document.createElement('label'),meter=document.createElement('meter');label.textContent=`${name}需求 ${d[key].score}`;meter.min=-100;meter.max=100;meter.value=d[key].score;meter.setAttribute('aria-label',`${name}需求`);label.append(meter);row.append(label,document.createTextNode(d[key].reasons.join('；')));demandRoot.append(row);}
 $('#population-policy').textContent=t.city.mode==='managed'?`城市經營：每 ${CENSUS_SECONDS} 遊戲秒評估一次，最多遷入一人；優先安置原有無家住戶。初住兩人可自備水；後續新住戶須有水井供水空位。缺水降低住宅需求。需求越低，等候越久。長期無家 ${GRACE.unhoused} 秒、失業 ${GRACE.unemployed} 秒或日用品不足 ${GRACE.unserved} 秒後，每次最多遷出一人；改善後停止該項倒數。累計遷入 ${t.demography.arrived} 人、遷出 ${t.demography.departed} 人。`:'自由營造：住宅直接入住，不因就業或日用品不足遷出；需求僅供營造參考。切換城市經營後，開始依需求與生活條件評估人口。';}
 function commerce(){const t=getTown(),a=t.city.trade,q=logistics(t);$('#trade-summary').textContent=`商業周轉金 ${formatMoney(a.funds)}；累計進口成本 ${formatMoney(a.importExpense)}、營業額 ${formatMoney(a.revenue)}、已繳成交稅 ${formatMoney(a.taxPaid)}。${a.funds<10?'周轉金不足，原料船暫停補貨；售出既有商品可回補。':''}`;
 $('#logistics-summary').textContent=`貨棧 ${q.level}／${MAX_LOGISTICS_LEVEL} 級：腳夫編制 ${q.porters} 人、每趟 ${q.porterLoad} 件；牛車 ${q.oxen} 輛、每趟 ${q.oxLoad} 件。每日物流維護 ${formatMoney(q.upkeep)}（經營模式）。`;
 const button=$('#logistics-upgrade');button.disabled=q.level>=MAX_LOGISTICS_LEVEL||!affordable(t,q.upgradeCost);button.textContent=q.level>=MAX_LOGISTICS_LEVEL?'貨棧已達最高三級':`貨棧升至 ${q.level+1} 級 · ${t.city.mode==='managed'?formatMoney(q.upgradeCost):'自由營造免付費'}`;
 const list=$('#trade-ledger');list.replaceChildren();for(const e of [...a.ledger].reverse().slice(0,12)){const li=document.createElement('li');li.textContent=`第 ${e.day+1} 日 · ${e.kind==='import'?'進口批次 '+e.batch:'成交'} ${GOODS[e.good]} ${e.quantity} 件 × ${formatMoney(e.unitPrice)}；稅 ${formatMoney(e.tax)}，周轉金 ${e.amount>=0?'+':'−'}${formatMoney(Math.abs(e.amount))}，餘額 ${formatMoney(e.balance)}`;list.append(li);}if(!a.ledger.length)list.textContent='尚無貿易收支。';}
 $('#buy-land').onclick=()=>{if(edit(buyLand))refresh();else $('#budget-warning').textContent='買地未完成：請確認為城市經營模式、鎮庫足夠且尚未買滿三區。';};
 const tabs=[...dialog.querySelectorAll('[role="tab"]')];
 function selectTab(tab){for(const t of tabs){const on=t===tab;t.setAttribute('aria-selected',String(on));document.getElementById(t.getAttribute('aria-controls')).hidden=!on;}}
 for(const tab of tabs)tab.onclick=()=>selectTab(tab);
 $('#logistics-upgrade').onclick=()=>{if(edit(upgradeLogistics))refresh();else $('#budget-warning').textContent='升級未完成：請確認鎮庫足夠且尚未滿三級。';};
 function refresh(){growth();commerce();const t=getTown(),c=t.city;$('#budget-summary').textContent=`${c.mode==='managed'?'城市經營':'自由營造'} · 鎮庫 ${formatMoney(c.treasury)}。${c.mode==='sandbox'?'若切換城市經營：':''}每日住稅預估 ${formatMoney(householdTax(t))}；榷酤 ${formatMoney(wineExcise(t))}（酒務${hasGovernment(t)?'':''}${wineExcise(t)?`，酒樓食肆 ${wineShops(t)} 處`:'未設'}）；成交稅 ${t.city.taxRate}%${transitTaxRate(t)?` ＋過稅 ${transitTaxRate(t)}%`:'（商稅務未設，無過稅）'}；目前每日維護費 ${formatMoney(dailyUpkeep(t))}。經營模式另依實際成交計稅。累計稅收 ${formatMoney(c.taxIncome)}、已付維護費 ${formatMoney(c.maintenancePaid)}。`;
 $('#budget-prices').textContent=`每格建設：民居 ${formatMoney(BUILD_COST.home)}、商鋪 ${formatMoney(BUILD_COST.shop)}、作坊 ${formatMoney(BUILD_COST.work)}、園景 ${formatMoney(BUILD_COST.garden)}；小路 ${formatMoney(ROAD_COST.lane)}、大路 ${formatMoney(ROAD_COST.avenue)}。四格按四格計費。每日每格維護：民居 ${formatMoney(UPKEEP.home)}、商鋪 ${formatMoney(UPKEEP.shop)}、作坊 ${formatMoney(UPKEEP.work)}、園景 ${formatMoney(UPKEEP.garden)}，再乘建築級數；小路 ${formatMoney(UPKEEP.lane)}、大路 ${formatMoney(UPKEEP.avenue)}。未落成建築暫不收維護費。`;
 const limit=townBounds(t),full=expansionOf(t)>=MAX_EXPANSION;
 $('#land-summary').textContent=`目前鎮界 ${boundsCells(limit)} 格（已買地 ${expansionOf(t)}／${MAX_EXPANSION} 區）。${full?'東側是汴河，已無地可買。':`再買一區向西擴兩列、南北各一列，需 ${formatMoney(landCost(t))}。`}`;
 const land=$('#buy-land');land.disabled=full||c.mode!=='managed';land.textContent=full?'已達最大鎮界':`買地擴城 · ${formatMoney(landCost(t))}`;
 $('#budget-mode').value=c.mode;$('#budget-tax').value=c.taxRate;$('#budget-warning').textContent=c.treasury<0?'鎮庫已欠款；供水與清運量降至75％，連續欠款第2日50％、第3日起25％。鎮庫回正即可恢復；可調整稅率、拆除高維護建築減支，或改為自由營造。':'城市經營須自行鋪路接通街坊，並收建設與維護費；自由營造自動接路、不扣款、不收稅。切換不重設鎮庫，也不追扣舊城建設費。';
 const list=$('#budget-ledger');list.replaceChildren();for(const entry of [...c.ledger].reverse()){const li=document.createElement('li');li.textContent=`第 ${entry.day+1} 日 · ${entry.label} ${entry.amount>=0?'+':'−'}${formatMoney(Math.abs(entry.amount))} · 餘額 ${formatMoney(entry.balance)}`;list.append(li);}if(!c.ledger.length)list.textContent='尚無收支紀錄。';}
 $('#budget-btn').onclick=()=>{refresh();dialog.showModal();};
 $('#budget-apply').onclick=()=>{const taxRate=Number($('#budget-tax').value),mode=$('#budget-mode').value;if(!Number.isInteger(taxRate)||taxRate<0||taxRate>20){$('#budget-warning').textContent='稅率請填 0 至 20 的整數。';return;}if(edit(t=>setCityPolicy(t,{mode,taxRate}))){dialog.close();}else $('#budget-warning').textContent='設定未儲存，原小鎮仍保留。';};
 return {update(){if(dialog.open){growth();commerce();}const c=getTown().city;$('#budget-btn').textContent=c.mode==='managed'?`鎮庫 ${formatMoney(c.treasury)} · 需求`:'自由營造 · 收支';},refresh};
}
