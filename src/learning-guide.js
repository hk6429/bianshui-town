import {questEntry} from './literary-quests.js';
import {learningQuest} from './learning.js';
import {activitiesDone} from './literary-activities.js';
const KEY='bianshui-learning-guide-v1';
export function learningChecklist(t,id){const s=learningQuest(t,id);return [{label:'讀三段，選證據並說理由',done:questEntry(t,id).step>=3,target:'.learning-reading'},{label:'完成三個場景活動',done:activitiesDone(t,id),target:'.literary-activity'},{label:'儲存一版自己的作品',done:s.revisions.length>0,target:'.learning-portfolio'},{label:'換情境說說看',done:s.reviews.some(r=>!r.kind||r.kind==='transfer'),target:'.learning-transfer'}];}
export function guideHTML(t,id){let dismissed=false;try{dismissed=localStorage.getItem(KEY)==='done';}catch{}const items=learningChecklist(t,id),next=items.find(x=>!x.done);return `<aside class="learning-guide"><h4>這一關怎麼玩？</h4><ol>${items.map(x=>`<li>${x.done?'✓':'○'} ${x.label}</li>`).join('')}</ol><button data-guide="next" data-target="${next?.target||'.learning-portfolio'}">${next?'帶我到下一步':'看看我的作品'}</button>${typeof location!=='undefined'&&new URLSearchParams(location.search).has('assignment')?'<button data-guide="class">交作業／看老師回饋</button>':''}<details ${dismissed?'':'open'}><summary>第一次玩的小提醒（可隨時重看）</summary><p>先讀一小段 → 選答案與一句線索 → 寫「因為……」。不會就按「需要線索」，答錯也能再試，不扣錢。</p><p>可以切換閱讀支援或聽朗讀；文字先口述，再請老師協助記錄。完成閱讀後做場景活動，最後把想法存成作品。</p><button data-guide="dismiss">我知道了，收起說明</button></details></aside>`;}
export function guideClick(event,dialog){const b=event.target.closest('[data-guide]');if(!b)return false;if(b.dataset.guide==='class'){dialog.close();document.querySelector('#classroom-cloud-btn')?.click();return true;}if(b.dataset.guide==='dismiss'){try{localStorage.setItem(KEY,'done');}catch{}b.closest('details').open=false;return true;}const target=dialog.querySelector(b.dataset.target);if(target){if(target.tagName==='DETAILS')target.open=true;target.scrollIntoView({block:'start',behavior:'auto'});const control=target.querySelector('input,select,textarea,button');control?.focus({preventScroll:true});}return true;}
