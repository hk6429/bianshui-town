# V10 華麗升級與重置 — 已發布

- 逐級增加整層寬簷樓閣；四級雙側亭樓，五級金瓦金冠與朱紅長幡。延用既有 tier 存檔。
- 29 種圖錄、47 種占地版本均逐級增高超過 0.6 世界單位；五級高度超過原型兩倍。47×5 模型有限值、幾何遞增、五級封頂檢查通過。
- 主畫面重置按鈕；確認預設聚焦保留；確認重置後復原上一步可救回。正式存檔不做破壞性測試。
- Chrome 同視角完成 1→5 級並保存截圖；取消重置保留原場景；390×844 手機重置 8 人／5 建築／5 街坊→0／0／0，再復原至 8／5／5，五級外觀保留。console error 0。
- 手機按鈕最初與統計卡重疊，改至卡片下方，截圖確認可見且可點。
- npm test 56/56；npm run build 與 git diff --check 通過。既有 JS 大於 500 KB 提示保留。
- 來源 6e121e3，Cloudflare 部署 5278935a；正式 7/7 檔案雜湊符合。
- 正式 Chrome 重置鍵、確認視窗及取消可用；26 人／26 建築／12 街坊保留，console error 0。
